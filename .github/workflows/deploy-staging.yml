
name: Deploy to Staging Server
on:
  push:
    branches:
      - test-deploy

jobs:

  deploy-to-staging:
    uses: ./.github/workflows/job.depoy.yml
    with:
      environment: Staging
      config_file: minetest.conf.test
      branch:      dev
    secrets: inherit
